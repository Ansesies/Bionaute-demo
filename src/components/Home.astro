---
// Home Component para Bionaute - Resumen profesional de la empresa
---

<section id="home-section" class="bg-gradient-to-b from-gray-50 to-white py-16 md:py-24">
  <div class="container mx-auto px-4 md:px-6">
    <!-- Encabezado Principal -->
    <div class="text-center mb-16 company-intro">
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-emerald-800 mb-6">
        Liderando la Revolución Biotecnológica
      </h2>
      <div class="w-24 h-1 bg-gradient-to-r from-emerald-600 to-teal-500 mx-auto mb-8"></div>
      <p class="text-lg md:text-xl text-gray-700 max-w-4xl mx-auto leading-relaxed">
        En <strong class="text-emerald-700">Bionaute</strong>, combinamos ciencia de vanguardia con innovación sostenible para transformar la agricultura moderna. Desarrollamos soluciones biotecnológicas que protegen los cultivos mientras preservan nuestro planeta para las futuras generaciones.
      </p>
    </div>

    <!-- Grid de Valores Principales -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mb-20">
      <!-- Valor 1: Excelencia Científica -->
      <div class="value-item bg-white p-8 rounded-xl shadow-lg hover:shadow-2xl transform hover:-translate-y-2 border border-emerald-50">
        <div class="bg-emerald-100 p-4 rounded-full w-16 h-16 flex items-center justify-center mb-6 mx-auto">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-emerald-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
          </svg>
        </div>
        <div class="border-b-2 border-emerald-200 w-12 mx-auto mb-4"></div>
        <h3 class="text-xl font-bold text-emerald-800 mb-4 text-center">Excelencia Científica</h3>
        <p class="text-gray-600 text-center leading-relaxed">
          Nuestro equipo de investigadores altamente calificados trabaja con las tecnologías más avanzadas para desarrollar soluciones que marcan la diferencia en el sector agrícola.
        </p>
      </div>

      <!-- Valor 2: Sostenibilidad -->
      <div class="value-item bg-white p-8 rounded-xl shadow-lg hover:shadow-2xl transform hover:-translate-y-2 border border-emerald-50">
        <div class="bg-teal-100 p-4 rounded-full w-16 h-16 flex items-center justify-center mb-6 mx-auto">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-teal-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h.5A2.5 2.5 0 0020 5.5v-1.65" />
          </svg>
        </div>
        <div class="border-b-2 border-teal-200 w-12 mx-auto mb-4"></div>
        <h3 class="text-xl font-bold text-emerald-800 mb-4 text-center">Compromiso Ambiental</h3>
        <p class="text-gray-600 text-center leading-relaxed">
          Cada producto que desarrollamos está diseñado con un enfoque ecológico, reduciendo el impacto ambiental y promoviendo prácticas agrícolas regenerativas.
        </p>
      </div>

      <!-- Valor 3: Innovación -->
      <div class="value-item bg-white p-8 rounded-xl shadow-lg hover:shadow-2xl transform hover:-translate-y-2 border border-emerald-50">
        <div class="bg-emerald-100 p-4 rounded-full w-16 h-16 flex items-center justify-center mb-6 mx-auto">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-emerald-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
        </div>
        <div class="border-b-2 border-emerald-200 w-12 mx-auto mb-4"></div>
        <h3 class="text-xl font-bold text-emerald-800 mb-4 text-center">Innovación Continua</h3>
        <p class="text-gray-600 text-center leading-relaxed">
          La investigación y el desarrollo son el corazón de nuestra empresa. Invertimos constantemente en nuevas tecnologías para estar a la vanguardia del sector.
        </p>
      </div>
    </div>

    <!-- Sección de Estadísticas/Logros -->
    <div class="bg-gradient-to-br from-emerald-900 to-teal-800 rounded-2xl p-8 md:p-12 text-white mb-20 stats-section">
      <div class="text-center mb-12">
        <h3 class="text-2xl md:text-3xl font-bold mb-4">Nuestro Impacto en Números</h3>
        <p class="text-emerald-100 text-lg max-w-2xl mx-auto">
          Más de una década de experiencia respalda nuestro compromiso con la excelencia y la innovación en biotecnología agrícola.
        </p>
      </div>
      
      <div class="grid md:grid-cols-4 gap-8 text-center">
        <div class="stat-item">
          <div class="text-3xl md:text-4xl font-bold text-green-300 mb-2">15+</div>
          <p class="text-emerald-100">Años de Experiencia</p>
        </div>
        <div class="stat-item">
          <div class="text-3xl md:text-4xl font-bold text-green-300 mb-2">500+</div>
          <p class="text-emerald-100">Proyectos Completados</p>
        </div>
        <div class="stat-item">
          <div class="text-3xl md:text-4xl font-bold text-green-300 mb-2">25+</div>
          <p class="text-emerald-100">Países Atendidos</p>
        </div>
        <div class="stat-item">
          <div class="text-3xl md:text-4xl font-bold text-green-300 mb-2">98%</div>
          <p class="text-emerald-100">Satisfacción del Cliente</p>
        </div>
      </div>
    </div>

    <!-- Misión y Visión -->
    <div class="grid lg:grid-cols-2 gap-12 mb-20">
      <!-- Misión -->
      <div class="mission-vision-item bg-white p-8 rounded-xl shadow-lg border-l-4 border-emerald-500">
        <div class="flex items-center mb-6">
          <div class="bg-emerald-100 p-3 rounded-full mr-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-emerald-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-emerald-800">Nuestra Misión</h3>
        </div>
        <p class="text-gray-600 leading-relaxed text-lg">
          Desarrollar y suministrar soluciones biotecnológicas innovadoras que maximicen la productividad agrícola mientras protegen el medio ambiente, contribuyendo a la seguridad alimentaria mundial de manera sostenible.
        </p>
      </div>

      <!-- Visión -->
      <div class="mission-vision-item bg-white p-8 rounded-xl shadow-lg border-l-4 border-teal-500">
        <div class="flex items-center mb-6">
          <div class="bg-teal-100 p-3 rounded-full mr-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-teal-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-emerald-800">Nuestra Visión</h3>
        </div>
        <p class="text-gray-600 leading-relaxed text-lg">
          Ser reconocidos como líderes globales en biotecnología agrícola, estableciendo nuevos estándares de sostenibilidad e innovación que transformen la agricultura hacia un futuro más próspero y respetuoso con el planeta.
        </p>
      </div>
    </div>

    <!-- Call to Action -->
    <div class="text-center cta-section">
      <h3 class="text-2xl md:text-3xl font-bold text-emerald-800 mb-6">
        ¿Listo para Transformar tu Agricultura?
      </h3>
      <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto">
        Descubre cómo nuestras soluciones biotecnológicas pueden impulsar tu productividad mientras cuidas el medio ambiente.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href="/services" 
          class="px-8 py-3 bg-gradient-to-r from-emerald-600 to-teal-500 text-white font-semibold rounded-lg hover:from-emerald-700 hover:to-teal-600 transform hover:-translate-y-1 transition-all duration-300 shadow-lg hover:shadow-emerald-500/30"
        >
          Explorar Servicios
        </a>
        <a 
          href="/contact" 
          class="px-8 py-3 border-2 border-emerald-600 text-emerald-700 font-semibold rounded-lg hover:bg-emerald-50 transform hover:-translate-y-1 transition-all duration-300"
        >
          Contactar Ahora
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animaciones personalizadas */
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }
  
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(40px); }
    to { opacity: 1; transform: translateY(0); }
  }
  
  @keyframes slideInLeft {
    from { opacity: 0; transform: translateX(-50px); }
    to { opacity: 1; transform: translateX(0); }
  }
  
  @keyframes slideInRight {
    from { opacity: 0; transform: translateX(50px); }
    to { opacity: 1; transform: translateX(0); }
  }
  
  @keyframes scaleIn {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }

  /* Estados iniciales para animaciones */
  .company-intro {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
  }
  
  .value-item {
    opacity: 0;
    transform: translateY(40px);
    transition: all 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }
  
  .stats-section {
    opacity: 0;
    transform: translateY(50px);
    transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
  }
  
  .stat-item {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }
  
  .mission-vision-item {
    opacity: 0;
    transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
  }
  
  .cta-section {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
  }

  /* Estados animados */
  .company-intro.in-view {
    opacity: 1;
    transform: translateY(0);
  }
  
  .value-item.in-view {
    opacity: 1;
    transform: translateY(0);
  }
  
  .stats-section.in-view {
    opacity: 1;
    transform: translateY(0);
  }
  
  .stat-item.in-view {
    opacity: 1;
    transform: translateY(0);
  }
  
  .mission-vision-item.in-view {
    opacity: 1;
    transform: translateX(0);
  }
  
  .cta-section.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  /* Animaciones secuenciales */
  .value-item:nth-child(1).in-view { transition-delay: 0s; }
  .value-item:nth-child(2).in-view { transition-delay: 0.2s; }
  .value-item:nth-child(3).in-view { transition-delay: 0.4s; }
  
  .stat-item:nth-child(1).in-view { transition-delay: 0.1s; }
  .stat-item:nth-child(2).in-view { transition-delay: 0.2s; }
  .stat-item:nth-child(3).in-view { transition-delay: 0.3s; }
  .stat-item:nth-child(4).in-view { transition-delay: 0.4s; }

  /* Animaciones direccionales para misión/visión */
  .mission-vision-item:nth-child(1) {
    transform: translateX(10px);
  }
  
  .mission-vision-item:nth-child(2) {
    transform: translateX(50px);
  }

  /* Efectos hover mejorados */
  .value-item:hover {
    transition: all 0.3s ease;
    box-shadow: 0 20px 40px -10px rgba(16, 185, 129, 0.2);
  }
  
  .stats-section {
    position: relative;
    overflow: hidden;
  }
  
  .stats-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, rgba(255,255,255,0.1) 0%, transparent 50%, rgba(255,255,255,0.1) 100%);
    background-size: 200% 200%;
    animation: shimmer 3s ease-in-out infinite;
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  
  .stats-section:hover::before {
    opacity: 1;
  }
  
  @keyframes shimmer {
    0% { background-position: -200% -200%; }
    50% { background-position: 200% 200%; }
    100% { background-position: -200% -200%; }
  }
</style>

<script>
  function initHomeAnimations() {
    // Configurar Intersection Observer para animaciones de scroll
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('in-view');
          
          // Para elementos con animación secuencial
          if (entry.target.classList.contains('stats-section')) {
            const statItems = entry.target.querySelectorAll('.stat-item');
            statItems.forEach((item, index) => {
              setTimeout(() => {
                item.classList.add('in-view');
              }, index * 150);
            });
          }
          
          if (entry.target.classList.contains('value-item')) {
            // Las animaciones secuenciales se manejan con CSS
          }
          
          // No volver a observar el elemento una vez animado
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    // Observar todos los elementos animables
    const animatedElements = document.querySelectorAll(
      '.company-intro, .value-item, .stats-section, .mission-vision-item, .cta-section'
    );
    
    animatedElements.forEach((el) => {
      observer.observe(el);
    });
  }

  // Inicializar animaciones cuando el DOM esté listo
  document.addEventListener('DOMContentLoaded', initHomeAnimations);
  
  // También inicializar en caso de navegación con View Transitions
  document.addEventListener('astro:page-load', initHomeAnimations);
</script>